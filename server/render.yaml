services:
  - type: web
    name: speakersense-backend
    env: docker
    plan: free
    dockerContext: ./server
    dockerfilePath: ./server/Dockerfile
    envVars:
      - key: GEMINI_API_KEY
        sync: false